# Copilot Instructions

This repository uses `AGENTS.md` as the canonical agent guide. Keep portable skill content in `plugins/strike/skills/` and keep host-specific metadata in the corresponding manifest directories.
