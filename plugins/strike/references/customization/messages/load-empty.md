# Strike Customization Packet

Skill: {{skill}}
Customization root: {{customization_root}}
Status: no repo-local customization loaded.

No user customization content was found for this skill.

If the current user asks for an extra doc/asset, create it only with clear
per-project or shared intent and a repo-safe save path. A repo-safe path is
relative, normalized inside the repo root, not absolute, not under `~`, not
using `..`, and not inside `.git/`, dependency, cache, or build-output folders
unless the user explicitly asks and the active skill allows it.
